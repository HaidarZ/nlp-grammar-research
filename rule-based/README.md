Before running make sure that you've installed the required libraries. You can do this by running the following command:

```bash
pip install -r requirements.txt
```

Then, install spacy trained pipeline by running the following command:

```bash
python -m spacy download en_core_web_lg
```